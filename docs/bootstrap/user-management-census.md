# User Management Census

Scoped service credentials are the leading runtime candidate. Proposed common behavior: issue once, verify a stored digest, bind owner/resource/scopes, expire, revoke, and list without revealing secrets. Browser-publishable identifiers carry a different authority contract. Opaque browser sessions should evaluate existing libraries. Universal users, organizations and authorization models are not established.
