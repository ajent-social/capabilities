# Workflow Census

Go validation, release verification, container artifacts and infrastructure previews are candidate contracts. Preserve required-check names, event coverage and product gates. Security defaults include minimal permissions, immutable action refs, constrained trust and isolated execution. No real consumer adoption has been verified.
