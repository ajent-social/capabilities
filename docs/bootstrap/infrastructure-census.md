# Infrastructure Census

Deployment identity is the first infrastructure candidate; private databases are a second boundary to specify. Combine provider-specific components, resource-level policy tests and live verification. No account, region, topology or source infrastructure is published. No components are deployed or claimed implemented.
