# Principles

Evidence before abstraction. Prefer standard libraries and maintained external implementations. Real consumers define APIs. Share the smallest coherent behavior, preserve product policy, and verify denied paths. Rejecting an unnecessary abstraction is success. Public claims never exceed public evidence or explicitly labelled maintainer attestations.
